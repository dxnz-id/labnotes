const Footer = () => {
    return (
        <footer className="fixed bottom-0 flex w-full items-center justify-center p-6">
            <p>Created with some ☕ | © {new Date().getFullYear()}</p>
        </footer>
    );
};

export default Footer;
